[vrs-client-service-force-table-test]
;Dial VRI
exten => 14120,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,890,1)
same => n,Hangup


;Dial VRI Web-public, mobile public
exten => 14124,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},EMG,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,890,1)
same => n,Hangup

[trunk-test-webrtc]
exten => 14150,1,Answer
same => n,Set(SOURCE_TYPE=WEB-PC)
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup

exten => 14151,1,Answer
same => n,Set(SOURCE_TYPE=WEB-PC)
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},EMG,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
;same => n,Playback(vrs-ivvr/waiting_short)
;same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Dial(SIP/00045)
same => n,Hangup

[tot-inbound-test]
;exten => _X!,1,,NoOp(Inbound TOT)
;same => n,NoOp(${CALLERID(all)})
;same => n,Dial(SIP/0979045000)
;same => n,Goto(test-ivr,s,1)
;same => n,Hangup()

; test new ttrs number 0979040001 - 0979041000
exten => _097904XXXX,1,NoOP()
same => n,NoOp(${CALLERID(all)})
same => n,Set(NETWORKSTATUS=${SIPPEER(${TRUNK_DIAL_TOT_OUT_FAILOVER:4},status)})
;same => n,Set(NETWORKSTATUS=${SIPPEER(${TRUNK_DIAL_TOT_OUT:4},status)})
same => n,SipAddHeader(P-Preferred-Identity:sip:${EXTEN}@10.0.17.154)
;same => n,Dial(SIP/0821210162@${TRUNK_DIAL_TOT_OUT_FAILOVER:4})
same => n,Dial(SIP/0645512205@trunk_tot2_out)
same => n,Hangup()

; Test new tot trunk
exten => 0979045001,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,VP,NORMAL,TEST)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Set(CALLERID(all)="${CALLERID(num)}" <${CALLERID(num)}>)
same => n,Goto(ext-queues,890,1)
same => n,Hangup()

[test-ivr]
exten => s,1,Answer()
same => n,Set(NUMTIMEOUTS=1])
same => n,Set(TIMEOUT(response)=10)
same => n,Set(TIMEOUT(digit)=5)
same => n,Background(yourcallisimportant)
same => n,WaitExten(5)

exten => t,1,NoOp(Number Try for Timeout = ${NUMTIMEOUTS})
same => n,GotoIf($[${NUMTIMEOUTS} < 2]?:thankyou)
same => n,Set(NUMTIMEOUTS=$[${NUMTIMEOUTS} + 1])
same => n,Goto(s,3)
same => n(thankyou),Playback(thank-you-for-calling)
same => n,Hangup()

exten => 1,1,NoOp(Press 1)
same => n,Playback(whoareyou)

exten => 2,1,NoOp(Press 2)
same => n,Playback(hello-world)

exten => 3,1,NoOp(Press 3)
same => n,Playback(thunderstorm)

exten => 4,1,NoOp(Press 4)
same => n,Playback(thirtieth)

exten => 5,1,NoOp(Press 5)
same => n,Playback(times)

exten => i,1,Goto(s,3)

exten => h,1,hangup()

[outbound-tot-test]

exten => _0XXXXXXXXX,1,NoOP()
same => n,NoOp(${CALLERID(all)})
same => n,SipAddHeader(P-Preferred-Identity:sip:${CALLERID(num)}@10.0.17.154)
same => n,Set(NETWORKSTATUS=${SIPPEER(trunk_tot2_out,status)})
same => n,GotoIf($[${REGEX("^OK" ${NETWORKSTATUS})}]?trunk1:trunk2)
same => n(trunk1),Dial(SIP/${EXTEN}@trunk_tot2_out)
same => n,Hangup()
same => n(trunk2),Dial(SIP/${EXTEN}@trunk_tot1_out)

exten => h,1,NoOP()
same => n,GotoIf($["${DIALSTATUS}"="CHANUNAVAIL"|"${DIALSTATUS}"="CONGESTION"|"${DIALSTATUS}"="CANCEL"|"${DIALSTTUS}"="automon"|"${DIALSTATUS}"="NOANSWER"|"${HANGUPCAUSE}"="0"|"${HANGUPCAUSE}"="34"]?failover)
same => n,Hangup()
same => n(failover),NoOP(FAIL OVER!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!11)
same => n,Hangup()
;;;Dial(SIP/${EXTEN:2}@SIP2,,tTo))

exten => _02XXXXXXX,1,NoOP()
same => n,SipAddHeader(P-Preferred-Identity:sip:${CALLERID(num)}@10.0.17.154)
same => n,Dial(SIP/${EXTEN}@trunk_tot2_out)



;;TEST 9'TON
[test-9ton]
exten => 0821210162,1,NoOp()
same => n,Set(CALLERID(all)=020260099)
same => n,Dial(SIP/0821210162@trunk_CAT_out,60,rtT)

exten => 0881078400,1,NoOp()
same => n,Set(CALLERID(all)=020260099)
same => n,Dial(SIP/0881078400@trunk_CAT_out,60,rtT)

[test-outbound-to-ems]
exten => _1669XX,1,NoOp(TEST EMS)
same => n,Answer
same => n,Dial(SIP/${EXTEN}@trunk_42_out,30,rtT)
same => n,Hangup()

[vrs-user-receiving-group-test]
exten => 020260001,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,VP,NORMAL,TEST)
same => n,Set(CALLERID(all)="Recieving <- ${CALLERID(num)} (Outside)" <${CALLERID(num)}>)
;same => n,Set(CALLERID(all)="0821210163" <0821210163>)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup()

exten => 1111160091348,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,VP,NORMAL,TEST)
same => n,Set(CALLERID(all)="Recieving <- ${CALLERID(num)} (Outside)" <${CALLERID(num)}>)
;same => n,Set(CALLERID(all)="0821210163" <0821210163>)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup()

[user-rev-test]
exten => 020260001,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,VP,NORMAL,TEST)
same => n,Set(CALLERID(all)="Recieving <- ${CALLERID(num)} (Outside)" <${CALLERID(num)}>)
;same => n,Set(CALLERID(all)="0821210163" <0821210163>)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup()

exten => 020260002,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,VP,NORMAL,TEST)
same => n,Set(CALLERID(all)="Recieving <- ${CALLERID(num)} (Outside)" <${CALLERID(num)}>)
;same => n,Set(CALLERID(all)="0821210164" <0821210164>)
same => n,Goto(ext-queues,${QUEUE_VRS_DEV},1)
same => n,Hangup()

exten => 3101300600407,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,VP,NORMAL,TEST)
same => n,Set(CALLERID(all)="Recieving <- ${CALLERID(num)} (Outside)" <${CALLERID(num)}>)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup()

exten => 020260010,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,Dial(SIP/020260010@ivr_out,30,rtT)
same => n,Hangup()

[vrs-inbound-ems]
;
;EMS Outbound
; 
; 020260090, 020260091, 020260092
;

include => vrs-ext-local-structure

exten => _X!,1,set(__CANCALLOUT=TRUE)
exten => _X!,2,set(__USER_CALLERID_MAPPING=${CALLERID(num)})
;exten => _X!,2,set(__USER_CALLERID_MAPPING=020260003)

;exten => _X!,1,NoOp(${CALLERID(num)})
;exten => _X!,2,DumpChan(3)
;exten => _X!,3,Hangup

exten => _141210,1,NoOp(Queue ems test)
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,MOBILE,EMG,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to EMS)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
;same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,001,1)


exten => _14121,1,NoOp(Queue ems test)
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,MOBILE,EMG,SERVICE)
same => n,NoOp(For override)
same => n,NoOp(Call to EMS)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
;same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)

;exten => _14121,1,NoOp(Queue ems test)
;same => n,SipAddHeader(P-Preferred-Identity:sip:0979045000@10.0.17.154)
;same => n,Dial(SIP/0875745601@${TRUNK_DIAL_TOT_OUT_FAILOVER:4})
;same => n,Hangup

exten => 0971405333,1,NoOp(Test)
;same => n,Set(CALLERID(all)="020260095 <020260095>")
;same => n,Dial(trunk_CAT_out/0971405333,60,rTt)
same => n,Set(CALLERID(all)="0979045045" <0979045045>)
same => n,SipAddHeader(P-Preferred-Identity:sip:0979045045@10.0.17.154)
same => n,Dial(SIP/0971405333@trunk_tot2_out)
same => n,Hangup()

[trunk-vrs-client-test]
;include => vrs-ext-local-structure-test
include => vrs-ext-main-service

exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

exten => _8888,1,Answer
same => n,Echo()
same => n,Hangup

exten => _X1412X,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,Hangup()

exten => _X1413X,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,Hangup()

exten => _X1414X,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,Hangup()

exten => _X1415X,1,NoOp(${EXTEN:0:1})
same => n,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,Hangup()

exten => _X1416X,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,Hangup()

exten => _X0XXXX!,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,Hangup()

exten => _A.,1,Set(SOURCE_TYPE=WEB-PC)
same => n,Goto(${EXTEN:2},1)

exten => _B.,1,Set(SOURCE_TYPE=MOBILE)
same => n,Goto(${EXTEN:2},1)

;;;test
exten => 14131,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,TEST)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
same => n,Set(__VQ_CIDPP="EMG <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_DEV},1)
same => n,Hangup

exten => 14130,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
same => n,Set(__VQ_CIDPP="VRI <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

exten => 14140,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

exten => 14150,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup

exten => 14151,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},EMG,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup


exten => 14120,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 14120,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,SERVICE)

;service emergency
exten => 1412,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 14121,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)

exten => _XXX!,2,set(__CANCALLOUT=FALSE)

;service vrs
exten => _0XXXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},NORMAL,SERVICE)
exten => _0XXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},NORMAL,SERVICE)
exten => _1412XXX!,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN:4},MAKING,${SOURCE_TYPE},NORMAL,SERVICE)

;service technician, helpdesk
exten => 14125,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},NORMAL,TECHNICIAN)
exten => 14127,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},NORMAL,HELPDESK)

;service emergency
exten => 1412,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 14121,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 1669,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 191,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 199,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},EMG,SERVICE)

;Dial nWise
exten => 14122,4,NoOp(Dial nWise!)
exten => 020260003,4,NoOp(Dial nWise!)

[vrs-operator-normal-group-test]
;
; Operator normal (Dial Plan)
;

include => vrs-ext-local-structure-test
include => vrs-ext-dnd-service

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

exten => _1669XX,1,NoOp(TEST EMS)
same => n,Answer
same => n,Dial(SIP/${EXTEN}@ems_main,30,rtT)
;same => n,Dial(SIP/${EXTEN}@ems_211,30,rtT)
same => n,Hangup()

exten => _142200,1,NoOp(TEST D1422)
same => n,Answer
same => n,Dial(SIP/${EXTEN}@d1422,30,rtT)
;same => n,Dial(SIP/${EXTEN}@d1422_dev,30,rtT)


; mcu
exten => _0000XX,1,Answer
same => n,AGI(${VRS_AGI_DIR}/agi_curl_destroy_mcu_room.php)
same => n,Dial(SIP/${EXTEN}@mcu_out)
same => n,Hangup

;Echo test
exten => _8888,1,Answer
same => n,Echo()
same => n,Hangup

;Echo test
exten => _7777,1,Answer
same => n,Echo()
same => n,Hangup

exten => _XXX!,1,set(__CANCALLOUT=TRUE)
exten => _XXX!,2,set(__OPERATOR_CALLERID=${CALLERID(num)})
exten => _XXX!,3,Set(__USER_UNIQUEID=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_uniqueid)})
exten => _XXX!,4,GotoIf($["${USER_UNIQUEID}" = ""]?next)
exten => _XXX!,5,Set(__USER_OUT_MAPPING=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_out_mapping)})
exten => _XXX!,6,Set(__USER_CALLERID_MAPPING=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_id)})
exten => _XXX!,7,Set(USER_CALL_TYPE=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_call_type)})
exten => _XXX!,8,Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "MAKING" | "${USER_CALL_TYPE}" = "VRI"]?"'VRS Call' <${AGENT_CALLER_NUM_DEFAULT}>")})
exten => _XXX!,9,GotoIf($["${USER_CALLERID_ALL}" = ""]?check_next:next)
;exten => _XXX!,10(check_next),Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "RECEIVING"]?"'Receiving Call' <${USER_CALLERID_MAPPING}>")})
exten => _XXX!,10(check_next),Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "RECEIVING"]?"${USER_CALLERID_MAPPING} <${USER_CALLERID_MAPPING}>")})


[vrs-user-normal-group-test]
;
; User VP normal (Dial Plan)
;

include => vrs-ext-main-service-test
include => vrs-ext-local-structure-test

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

; mcu
exten => _0000XX,1,Answer
same => n,Dial(SIP/${EXTEN}@mcu_out)
same => n,Hangup

exten => _XXX!,2,set(__CANCALLOUT=FALSE)

;service vrs
exten => _0XXXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,TEST)
exten => _0XXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,TEST)
exten => _1412XXX!,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN:4},MAKING,VP,NORMAL,TEST)

;service vri
exten => 14120,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,VP,NORMAL,TEST)

;service technician, helpdesk
exten => 14125,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,NORMAL,TECHNICIAN)
exten => 14127,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,NORMAL,HELPDESK)

;service emergency
exten => 1412,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,TEST)
exten => 14121,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,TEST)
exten => 1669,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,TEST)
exten => 191,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,TEST)
exten => 199,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,TEST)

;Dial nWise
exten => 14122,4,NoOp(Dial nWise!)
exten => 020260003,4,NoOp(Dial nWise!)

[vrs-user-kiosk-test]
;
; User Kiosk (Dial Plan)
;

include => vrs-ext-main-service-kiosk-test

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup


exten => _XXX!,2,set(__CANCALLOUT=FALSE)

;service vrs
exten => _0XXXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,NORMAL,TEST)
exten => _0XXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,NORMAL,TEST)
exten => _XXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,NORMAL,TEST)

;service vri
exten => 14120,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,KIOSK,NORMAL,TEST)

;service emergency
exten => 1669,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,EMG,TEST)
exten => 191,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,EMG,TEST)
exten => 199,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,EMG,TEST)


[vrs-ext-local-structure-test]
;
; Dial local 
;


exten => _XXX!,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n(next),Set(__USER_CALL=${CALLERID(num)})

same => n,Set(LOCAL_EXTENSION=${DB(DEVICE/${EXTEN}/dial)})

same => n,GotoIf($[${LEN(${EXTEN})}=13]?step1);;NEW
same => n,GotoIf($[${REGEX("^0979045[0-2][0-9]{2}" ${EXTEN})}]?step1)
same => n,GotoIf($[${REGEX("^02026009[0-3,6-8]" ${EXTEN})}]?step1:external)
same => n(step1),Goto(vrs-user-client-server-call,${EXTEN},1);;NEW

same => n(external),GotoIf($["${LOCAL_EXTENSION}" = ""]?vrs-ext-external-call-test,${EXTEN},1);;NEW
same => n,Set(__IS_LOCAL_DIAL=TRUE)
same => n,Set(CALLERID(all)=${IF($["${USER_CALLERID_ALL}" = ""]?${CALLERID(all)}:${USER_CALLERID_ALL})})
same => n,Set(SRC_CALLERID=${IF($["${OPERATOR_CALLERID}" = ""]?${CALLERID(num)}:${OPERATOR_CALLERID})})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dial.php,${SRC_CALLERID},${USER_UNIQUEID})
;;Pushkit iOS
same => n,GotoIf($["${REGEX("pn-type=apple" ${DB(SIP/Registry/${EXTEN})})}"="1"]?ios:normal)
same => n,NoOp(iOS Dial)
same => n(ios),Set(PRESENCE_STATE(CustomPresence:${EXTEN})=available,,)
same => n,AGI(${PUSHKIT_DIR}/pushkit.py, ${DB(SIP/Registry/${EXTEN})})
same => n,Wait(6)
;same => n,Dial(Local/${EXTEN}@ext-local,60,rTt)
same => n,Dial(Local/${EXTEN}@ext-local)
same => n,Hangup()
same => n(normal),NoOp(Normal Dial)
same => n,Dial(${LOCAL_EXTENSION},30,rTt)
same => n(hangup),Hangup()

exten => h,1,Macro(vrs-hangupcall,)

[vrs-ext-external-call-test]
;
; Dial external 
;

exten => _X!,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,GotoIf($["${CANCALLOUT}" = "TRUE"]?callout)
same => n,NoOp(No Permission to directly call out)
same => n,Hangup(29)

;
;Set Callerid call out
;Callerid range 020260001-020260100 and 020261000-020261099
;
same => n(callout),NoOp(USER_CALLERID_MAPPING=${USER_CALLERID_MAPPING})
same => n,GotoIf($["${USER_CALLERID_MAPPING}" = ""]?setdefault)
same => n,GotoIf($[${REGEX("^0979045[0-2][0-9]{2}" ${USER_CALLERID_MAPPING})}]?setcaller)
same => n,GotoIf($[${REGEX("^02026000[1-9]|^0202600[1-9][0-9]|^020260100" ${USER_CALLERID_MAPPING})}]?setcaller)
same => n,GotoIf($[${REGEX("^0202610[0-8][0-9]|^02026109[0-9]" ${USER_CALLERID_MAPPING})}]?setcaller:setdefault)
same => n(setcaller),Set(CALLERID(all)=${USER_CALLERID_MAPPING})
same => n,Goto(continue)

same => n(setdefault),Set(CALLERID(all)=${DEFAULTCALLERID})
same => n(continue),NoOp(CDR Billing Here!)
same => n,Set(__IS_EXTERNAL_DIAL=TRUE)
same => n,Set(__DIAL_EXTENSION=${EXTEN})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dial.php,${USER_CALL},${USER_UNIQUEID})
same => n,Gosub(vrs-sub-record-dial,s,1())

;; filter tot trunk
same => n,NoOp(number=${CALLERID(num)})
same => n,GotoIf($[${REGEX("^0979045[0-2][0-9]{2}" ${CALLERID(num)})}]?tot_trunk)

same => n,Dial(${TRUNK_DIAL_OUT}/${EXTEN},60,rtT)
same => n,Hangup()

same => n(tot_trunk),NoOp(${CALLERID(all)})
same => n,SipAddHeader(P-Preferred-Identity:sip:${CALLERID(num)}@${VRIVATE_IP_TOT})
same => n,Dial(${TRUNK_DIAL_TOT_OUT}/${EXTEN},60,rtT)
same => n,Hangup()

exten => h,1,Macro(vrs-hangupcall,)

;;New
[vrs-user-client-server-call]
;
; Dial client server
;

exten => _X!,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(CALLERID(all)=${IF($["${USER_CALLERID_ALL}" = ""]?${CALLERID(all)}:${USER_CALLERID_ALL})})
same => n,Set(SRC_CALLERID=${IF($["${OPERATOR_CALLERID}" = ""]?${CALLERID(num)}:${OPERATOR_CALLERID})})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dial.php,${SRC_CALLERID},${USER_UNIQUEID})
same => n,Gosub(vrs-sub-record-dial,s,1())
same => n,NoOp(CALLERID=${CALLERID(all)})

;; filter tot trunk
same => n,NoOp(number=${CALLERID(num)})
same => n,GotoIf($[${REGEX("^0979045[0-2][0-9]{2}" ${EXTEN})}]?tot_trunk)

same => n,Dial(${VRS_CLIENT_TRUNK_OUT}/${EXTEN},60,rtT)
same => n,Hangup()

same => n(tot_trunk),NoOp(${CALLERID(all)})
same => n,SipAddHeader(P-Preferred-Identity:sip:${CALLERID(num)}@${VRIVATE_IP_TOT})
same => n,Dial(${VRS_CLIENT_TRUNK_OUT}/${EXTEN},60,rtT)
same => n,Hangup()

exten => h,1,Macro(vrs-hangupcall,)

[vrs-ext-main-service-test]
;
; VP services (dial plan)
;

;Dial Emergency
exten => 1412,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="EMG <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial Emergency
exten => 14121,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="EMG <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup


;Dial VRI
exten => 14120,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short_3s)
;same => n,Playback(record/0)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial nWis
exten => 14122,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
same => n,Answer()
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,Dial(SIP/ttrs@trunk_nWise-production,60,rtT)

;Dial nWis
exten => 020260003,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
same => n,Answer()
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,Dial(SIP/ttrs@trunk_nWise-production,60,rtT)

;Dial VRS TECHNICIAN
exten => 14125,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to Technician)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="Technician <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial VRS HELP DESK
exten => 14127,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to Helpdesk)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-helpdesk-sub,s,1())
;same => n,Set(__VQ_CIDPP="HelpDesk <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup


;Dial VRS
exten => _1412XXX!,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN:4} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial VRS 08
exten => _0XXXXXXXXX,1,NoOp()
same => n,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial VRS 02
exten => _0XXXXXXXX,1,NoOp()
same => n,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial 4 Digit
exten => _XXXX,1,NoOp()
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;:::Emergency Thai:::

;Medical Emergency
exten => 1669,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Police Emergency
exten => 191,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Fire Emergency
exten => 199,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

exten => h,1,Macro(vrs-hangupcall,)

[vrs-ext-main-service-kiosk-test]
;
; Kiosk services (dial plan)
;

;Dial VRI
exten => 14120,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial VRS 08
exten => _0XXXXXXXXX,1,NoOp()
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial VRS 02
exten => _0XXXXXXXX,1,NoOp()
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial 4 Digit
exten => _XXXX,1,NoOp()
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;:::Emergency Thai:::

;Medical Emergency
exten => 1669,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Police Emergency
exten => 191,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Fire Emergency
exten => 199,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-test,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
;same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

exten => h,1,GotoIf($["${USER_UNIQUEID}" != "${UNIQUEID}"]?next)
same => n,AGI(${VRS_AGI_DIR}/agi_update_log_service.php,1,${USER_UNIQUEID},${USER_SOURCE_TYPE},${EPOCH})
same => n(next),Hangup

[vrs-check-service-off-main-sub-test]
;
; VRS service time off
;

exten => s,1,NoOp()
;same => n,GotoIfTime(23:00-07:00,*,*,*?vrs-ext-main-service-off,${EXTEN},1)
same => n,Return()

[vrs-check-service-off-helpdesk-sub-test]
;
; Help Desk service time off
;

exten => s,1,NoOp()
same => n,GotoIfTime(17:30-08:30,mon-fri,*,*?vrs-ext-main-service-off-helpdesk,${EXTEN},1)
same => n,GotoIfTime(*,sat-sun,*,*?vrs-ext-main-service-off-helpdesk,${EXTEN},1)
same => n,Return()
