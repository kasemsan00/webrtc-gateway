; for testing context
#include extensions_custom_agi_send_event.conf
#include extensions_custom_check_service_off.conf
#include extensions_custom_check_service_off_receiving.conf
#include extensions_custom_dev.conf
#include extensions_custom_test.conf
#include extensions_custom_ttrstest.conf
#include extensions_custom_transfer.conf
#include extensions_custom_trunk_ivr.conf
#include extensions_custom_trunk_speech_enhancer.conf
#include extensions_custom_trunk_vrs_client.conf
#include extensions_custom_vrs_client_service.conf
#include extensions_custom_vrs_check_block.conf
#include extensions_custom_webrtc.conf

exten => record,1,Answer
same => n,Wait(3)
same => n,Record(record/0_3s.wav);;Record file
same => n,Playback(record/0_3s);;Playback test
;same => n,Playback(exsound_fifth);;Playback test
same => n,HangUp


exten => test,1,Answer
;same => n,Wait(6)
;same => n,Record(/var/lib/asterisk/sounds/vrs-ivvr/vrs_service_off_no_time.wav);;Record file
same => n,Playback(vrs-ivvr/vrs_service_off_no_time);;Playback test
;same => n,Playback(vrs-ivvr/vrs_service_off)
;same => n,Playback(vrs-ivvr/vrs_service_off_0700_2000)
;same => n,Playback(vrs-ivvr/vrs_service_off_repairing)
;same => n,Wait(1)
same => n,HangUp

exten => 11999,1,Answer
same => n,Echo()
same => n,HangUp

exten => 11777,1,Answer
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Playback(exsound_more_than_tenth)
same => n,HangUp

exten => 11888,1,Answer
same => n,Set(SOURCE_TYPE=WEB-PC)
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Wait(3)
same => n,Playback(vrs-ivvr/waiting_short)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)

exten => 11222,1,Answer
same => n,Wait(3)
same => n,Playback(vrs-ivvr/output_test640p);;Playback test
same => n,HangUp

exten => 11333,1,Answer
same => n,Wait(3)
same => n,Playback(vrs-ivvr/output_test720p);;Playback test
same => n,HangUp

exten => 11444,1,Answer
same => n,Wait(3)
same => n,Playback(vrs-ivvr/output_test1080p);;Playback test
same => n,HangUp

exten => 9999,1,Answer
;same => n,Gosub(check-block-by-system,s,1(0971405333))
;same => n,Set(SOURCE_NUMBER=)
;same => n,Set(DESTINATION_NUMBER=0975045049)
;same => n,Set(SOURCE_TYPE_IQM=MOBILE)
;same => n,Set(BOUND_TYPE=outbound)
;same => n,AGI(agi://127.0.0.1/check_block,outbound,MOBILE,1430500238566,0934281521)
;same => n,AGI(${VRS_AGI_DIR}/agi_check_block.php,outbound,MOBILE,8671184014248,0927159643)
;same => n,NoOp(BLOCK=${BLOCK})
same => n,Echo()
;same => n,Set(TO=sip:0000168180001)
;same => n,GotoIf($[${REGEX("^00001681800[0-9]{2}" ${TO:4})}]?step1:step2)
;same => n(step1),NoOp(step1)
same => n,Hangup
same => n(step2),NoOp(step2)

same => n,Hangup

exten => 8888,1,Answer()
same => n,NoOp(To ${MESSAGE(to)})
same => n,NoOp(From ${MESSAGE(from)})
same => n,Set(MESSAGE(body)=test message)
same => n,MessageSend(sip:8888@${VRS_MOBILE_CLIENT_IP},"8888" <sip:8888@${VRS_MOBILE_CLIENT_IP}>)
same => n,Hangup

exten => 6666,1,NoOp(Test)
same => n,Set(CALLERID(all)="0830034100 <0830034100>")
same => n,Dial(SIP/0821210162@ttrs_number,60,rTt)
same => n,Hangup()

exten => _0645512205,1,NoOP()
same => n,Set(CALLERID(all)="0979045045" <0979045045>)
same => n,SipAddHeader(P-Preferred-Identity:sip:0979045045@10.0.17.154)
same => n,Dial(SIP/0645512205@trunk_tot2_out)
same => n,Hangup()

exten => _0821210162,1,NoOP()
same => n,Set(CALLERID(all)="0979045045" <0979045045>)
same => n,SipAddHeader(P-Preferred-Identity:sip:0979045045@10.0.17.154)
same => n,Dial(SIP/0821210162@trunk_tot2_out)
same => n,Hangup()

; for real context

[vrs-technician]
;
; technician (Dial Plan)
;

include => vrs-ext-main-service
include => vrs-ext-local-structure

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

; AGI ARGV Format
; (destination_number, call_type, source_type, priority_type, group_type)
; Ex: (${EXTEN}, 1, 1, 2, 1)
;
; call_type : 1=MAKING, 2=RECEIVING, 3=VRI
; source_type : 1=VP, 2=KIOSK, 3=MOBILE, 4=WEB, 5=CAPTION
; priority_type : 1=NORMAL, 2=EMG
; group_type : 1=SERVICE, 2=HELPDESK, 3=TECHNICIAN, 4=TEST

exten => _XXX!,2,set(__CANCALLOUT=FALSE)

;exten => _90ZX!,2,set(__CANCALLOUT=TRUE)
;exten => _90ZX!,3,set(__DEFAULTCALLERID=020260095)
;exten => _90ZX!,4,Goto(vrs-ext-local-structure,${EXTEN:1},1)

exten => _9XXX!,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _9XXX!,3,set(__CANCALLOUT=TRUE)
exten => _9XXX!,4,set(__DEFAULTCALLERID=020260095)
exten => _9XXX!,5,Goto(vrs-ext-local-structure,${EXTEN:1},1)

;service vrs
exten => _0XXXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,SERVICE)
exten => _0XXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,SERVICE)
exten => _1412XXX!,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN:4},MAKING,VP,NORMAL,SERVICE)

;service vri
exten => 14120,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,VP,NORMAL,SERVICE)

;service technician, helpdesk
exten => 14125,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,NORMAL,TECHNICIAN)
exten => 14127,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,NORMAL,HELPDESK)

;service emergency
exten => 1412,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)
exten => 14121,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)
exten => 1669,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,SERVICE)
exten => 191,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,SERVICE)
exten => 199,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,SERVICE)

;Dial nWise
exten => 14122,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)
exten => 020260003,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)


[trunk-vrs-client-test]
;include => vrs-ext-local-structure-test
include => vrs-ext-main-service

exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

exten => _8888,1,Answer
same => n,Echo()
same => n,Hangup

exten => _X1412X,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "3"]?Goto(K${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "4"]?Goto(V${EXTEN},1));For VP Dial Queue
same => n,Hangup()

;For VP Dial Queue
exten => _X1412XXX!,1,ExecIf($["${EXTEN:0:1}" = "4"]?Goto(V${EXTEN},1))
same => n,Hangup()

;For Group test
exten => _X1413X,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "3"]?Goto(K${EXTEN},1))
same => n,Hangup()

;For Group test
exten => _X1414X,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,Hangup()

;For Group test
exten => _X1415X,1,NoOp(${EXTEN:0:1})
same => n,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "3"]?Goto(K${EXTEN},1))
same => n,Hangup()

exten => _X1416X,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,Hangup()

exten => _X0XXXX!,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "3"]?Goto(K${EXTEN},1));For KIOSK Dial Queue
same => n,ExecIf($["${EXTEN:0:1}" = "4"]?GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1));For VP Dial local
same => n,ExecIf($["${EXTEN:0:1}" = "4"]?Goto(V${EXTEN},1));For VP Dial Queue
same => n,Hangup()

exten => _XXXXX,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "3"]?Goto(K${EXTEN},1));For KIOSK Dial Queue
same => n,ExecIf($["${EXTEN:0:1}" = "4"]?GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1));For VP Dial local
same => n,ExecIf($["${EXTEN:0:1}" = "4"]?Goto(V${EXTEN},1));For VP Dial Queue
same => n,Hangup()

exten => _X191,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "3"]?Goto(K${EXTEN},1));For KIOSK Dial Queue
same => n,ExecIf($["${EXTEN:0:1}" = "4"]?GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1));For VP Dial local
same => n,ExecIf($["${EXTEN:0:1}" = "4"]?Goto(V${EXTEN},1));For VP Dial Queue
same => n,Hangup()

exten => _X199,1,ExecIf($["${EXTEN:0:1}" = "1"]?Goto(A${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "2"]?Goto(B${EXTEN},1))
same => n,ExecIf($["${EXTEN:0:1}" = "3"]?Goto(K${EXTEN},1));For KIOSK Dial Queue
same => n,ExecIf($["${EXTEN:0:1}" = "4"]?GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1));For VP Dial local
same => n,ExecIf($["${EXTEN:0:1}" = "4"]?Goto(V${EXTEN},1));For VP Dial Queue
same => n,Hangup()

;VP dial Local 
exten => _X1669XXX,1,ExecIf($["${EXTEN:0:1}" = "4"]?Goto(vrs-ext-local-structure,${EXTEN:1},1));For VP Dial local
same => n,Hangup()

exten => _A.,1,Set(SOURCE_TYPE=WEB-PC)
same => n,Goto(${EXTEN:2},1)

exten => _B.,1,Set(SOURCE_TYPE=MOBILE)
same => n,Goto(${EXTEN:2},1)

exten => _V.,1,Set(SOURCE_TYPE=VP)
same => n,Goto(${EXTEN:2},1)

exten => _K.,1,Goto(vrs-user-kiosk,${EXTEN:2},1)

;;;test
exten => 14131,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,TEST)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
same => n,Set(__VQ_CIDPP="EMG <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_DEV},1)
same => n,Hangup

exten => 14130,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
same => n,Set(__VQ_CIDPP="VRI <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_DEV},1)
same => n,Hangup

exten => 14140,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

exten => 14150,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup

exten => 14151,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},EMG,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup


exten => 14120,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 14120,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,SERVICE)

;service emergency
exten => 1412,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 14121,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)

exten => _XXX!,2,set(__CANCALLOUT=FALSE)

;service vrs
exten => _0XXXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},NORMAL,SERVICE)
exten => _0XXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},NORMAL,SERVICE)
exten => _XXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _XXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},NORMAL,SERVICE)
exten => _1412XXX!,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN:4},MAKING,${SOURCE_TYPE},NORMAL,SERVICE)

;service technician, helpdesk
exten => 14125,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},NORMAL,TECHNICIAN)
exten => 14127,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},NORMAL,HELPDESK)

;service emergency
exten => 1412,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 14121,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 1669,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 191,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 199,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,${SOURCE_TYPE},EMG,SERVICE)

;Dial nWise
exten => 14122,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)
exten => 020260003,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${SOURCE_TYPE},EMG,SERVICE)


; for real context

[vrs-operator-normal]
;
; Operator normal (Dial Plan)
;

include => vrs-ext-local-structure
include => vrs-ext-dnd-service

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

exten => _142200,1,NoOp(TEST D1422)
same => n,Answer
same => n,Dial(SIP/${EXTEN}@d1422,30,rtT)

exten => _XXX!,1,set(__CANCALLOUT=TRUE)
exten => _XXX!,2,set(__OPERATOR_CALLERID=${CALLERID(num)})
exten => _XXX!,3,Set(__USER_UNIQUEID=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_uniqueid)})
exten => _XXX!,4,GotoIf($["${USER_UNIQUEID}" = ""]?next)
exten => _XXX!,5,Set(__USER_OUT_MAPPING=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_out_mapping)})
exten => _XXX!,6,Set(__USER_CALLERID_MAPPING=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_id)})
exten => _XXX!,7,Set(USER_CALL_TYPE=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_call_type)})
exten => _XXX!,8,Set(USER_SOURCE_TYPE=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_source_type)})
exten => _XXX!,9,Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "MAKING" | "${USER_CALL_TYPE}" = "VRI"]?"'VRS Call' <${AGENT_CALLER_NUM_DEFAULT}>")})
exten => _XXX!,10,GotoIf($["${USER_CALLERID_ALL}" = ""]?check_next:next)
;exten => _XXX!,11(check_next),Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "RECEIVING"]?"'Receiving Call' <${USER_CALLERID_MAPPING}>")})
exten => _XXX!,11(check_next),Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "RECEIVING"]?"${USER_CALLERID_MAPPING} <${USER_CALLERID_MAPPING}>")})

[vrs-operator-softphone]
;
; Operator VP, Kiosk Softphone (Dial Plan)
;

include => vrs-ext-local-structure
include => vrs-ext-dnd-service

exten => _*9999,1,NoOp(test)
same => n,NoOp()
same => n,AGI(${VRS_AGI_DIR}/agi_test.php)
; same => n,Hangup()
; same => n,Hangup(17)
; same => n,Busy()

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

exten => _142200,1,NoOp(TEST D1422)
same => n,Answer
same => n,Dial(SIP/${EXTEN}@d1422,30,rtT)

; mcu
exten => _0XXXX,1,Answer
same => n,AGI(${VRS_AGI_DIR}/agi_curl_destroy_mcu_room.php)
same => n,Dial(SIP/${EXTEN}@mcu_out,60,rtT)
same => n,Hangup

exten => _0XXXXX,1,Answer
same => n,AGI(${VRS_AGI_DIR}/agi_curl_destroy_mcu_room.php)
same => n,Dial(SIP/${EXTEN}@mcu_out,60,rtT)
same => n,Hangup

exten => _XXX!,1,set(__CANCALLOUT=TRUE)
exten => _XXX!,2,set(__OPERATOR_CALLERID=${CALLERID(num)})
exten => _XXX!,3,Set(__USER_UNIQUEID=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_uniqueid)})
exten => _XXX!,4,GotoIf($["${USER_UNIQUEID}" = ""]?next)
exten => _XXX!,5,Set(__USER_OUT_MAPPING=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_out_mapping)})
exten => _XXX!,6,Set(__USER_CALLERID_MAPPING=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_id)})
exten => _XXX!,7,Set(USER_CALL_TYPE=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_call_type)})
exten => _XXX!,8,Set(USER_SOURCE_TYPE=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_source_type)})
exten => _XXX!,9,Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "MAKING" | "${USER_CALL_TYPE}" = "VRI"]?"'VRS Call' <${AGENT_CALLER_NUM_DEFAULT}>")})
exten => _XXX!,10,GotoIf($["${USER_CALLERID_ALL}" = ""]?check_next:next)
;exten => _XXX!,11(check_next),Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "RECEIVING"]?"'Receiving Call' <${USER_CALLERID_MAPPING}>")})
exten => _XXX!,11(check_next),Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "RECEIVING"]?"${USER_CALLERID_MAPPING} <${USER_CALLERID_MAPPING}>")})

[textchat-operator-softphone]
;
; Operator Text Chat (Dial Plan)
;

include => vrs-ext-local-structure
include => vrs-ext-dnd-service

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

exten => _XXX!,1,set(__CANCALLOUT=TRUE)
exten => _XXX!,2,Set(__DEFAULTCALLERID=020260002)
exten => _XXX!,3,Set(__USER_CALLERID_PREFIX=1412)
exten => _XXX!,4,set(__OPERATOR_CALLERID=${CALLERID(num)})
exten => _XXX!,5,Set(__USER_UNIQUEID=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_uniqueid)})
exten => _XXX!,6,GotoIf($["${USER_UNIQUEID}" = ""]?next)
exten => _XXX!,7,Set(__USER_OUT_MAPPING=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_out_mapping)})
exten => _XXX!,8,Set(__USER_CALLERID_MAPPING=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_id)})
exten => _XXX!,9,Set(USER_CALL_TYPE=${DB(VRS_VARIABLE/${OPERATOR_CALLERID}/user_call_type)})
exten => _XXX!,10,Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "MAKING" | "${USER_CALL_TYPE}" = "VRI"]?"'VRS Call' <${AGENT_CALLER_NUM_DEFAULT}>")})
exten => _XXX!,11,GotoIf($["${USER_CALLERID_ALL}" = ""]?check_next:next)
;exten => _XXX!,12(check_next),Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "RECEIVING"]?"'Receiving Call' <${USER_CALLERID_MAPPING}>")})
exten => _XXX!,12(check_next),Set(__USER_CALLERID_ALL=${IF($["${USER_CALL_TYPE}" = "RECEIVING"]?"${USER_CALLERID_MAPPING} <${USER_CALLERID_MAPPING}>")})

[vrs-operator-api]
include => vrs-ext-local-structure

exten => _XXX!,1,set(__CANCALLOUT=TRUE)
exten => _XXX!,2,set(__OPERATOR_CALLERID=${CALLERID(num)})
exten => _XXX!,3,AGI(${VRS_AGI_DIR}/agi_set_variablemapping_softphone.php)

[vrs-user-normal]
;
; User VP normal (Dial Plan)
;

include => vrs-ext-main-service
include => vrs-ext-local-structure

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

; AGI ARGV Format
; (destination_number, call_type, source_type, priority_type, group_type)
; Ex: (${EXTEN}, 1, 1, 2, 1)
;
; call_type : 1=MAKING, 2=RECEIVING, 3=VRI
; source_type : 1=VP, 2=KIOSK, 3=MOBILE, 4=WEB, 5=CAPTION
; priority_type : 1=NORMAL, 2=EMG
; group_type : 1=SERVICE, 2=HELPDESK, 3=TECHNICIAN, 4=TEST

exten => _XXXXX!,2,set(__CANCALLOUT=FALSE)

;service vrs
exten => _0XXXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,SERVICE)
exten => _0XXXXXXXX,2,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,SERVICE)
exten => _1412XXX!,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN:4},MAKING,VP,NORMAL,SERVICE)

exten => _XXXX,3,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _XXXX,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,SERVICE)

;service vri
exten => 14120,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,VP,NORMAL,SERVICE)

;service technician, helpdesk
exten => 14125,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,NORMAL,TECHNICIAN)
exten => 14127,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,NORMAL,HELPDESK)

;service emergency
exten => 1412,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)
exten => 14121,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)
exten => 1669,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,SERVICE)
exten => 191,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,SERVICE)
exten => 199,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,SERVICE)

;Dial nWise
exten => 14122,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)
exten => 020260003,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)

[vrs-user-kiosk]
;
; User Kiosk (Dial Plan)
;

include => vrs-ext-main-service-kiosk

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

; AGI ARGV Format
; (destination_number, call_type, source_type, priority_type, group_type)
; Ex: (${EXTEN}, 1, 1, 2, 1)
;
; call_type : 1=MAKING, 2=RECEIVING, 3=VRI
; source_type : 1=VP, 2=KIOSK, 3=MOBILE, 4=WEB, 5=CAPTION
; priority_type : 1=NORMAL, 2=EMG
; group_type : 1=SERVICE, 2=HELPDESK, 3=TECHNICIAN, 4=TEST

exten => _XXX!,2,set(__CANCALLOUT=FALSE)

;service vrs
exten => _0XXXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,NORMAL,SERVICE)
exten => _0XXXXXXXX,3,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,NORMAL,SERVICE)
exten => _XXXX,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,NORMAL,SERVICE)

;service vri
exten => 14120,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,KIOSK,NORMAL,SERVICE)

;service emergency
;exten => 1669,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,EMG,SERVICE)
;exten => 191,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,EMG,SERVICE)
;exten => 199,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,EMG,SERVICE)

exten => 1669,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,NORMAL,SERVICE)
exten => 191,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,NORMAL,SERVICE)
exten => 199,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,KIOSK,NORMAL,SERVICE)

exten => 14150,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,KIOSK,NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup


[vrs-user-public-plus]
;
; User VP 02026XXXX (Dial Plan)
;


include => vrs-ext-main-service
include => vrs-ext-local-structure

;Echo test
exten => _9999,1,Answer
same => n,Echo()
same => n,Hangup

; AGI ARGV Format
; (destination_number, call_type, source_type, priority_type, group_type)
; Ex: (${EXTEN}, 1, 1, 2, 1)
;
; call_type : 1=MAKING, 2=RECEIVING, 3=VRI
; source_type : 1=VP, 2=KIOSK, 3=MOBILE, 4=WEB, 5=CAPTION
; priority_type : 1=NORMAL, 2=EMG
; group_type : 1=SERVICE, 2=HELPDESK, 3=TECHNICIAN, 4=TEST

;service vrs
exten => _0XXXXXXXXX,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => _0XXXXXXXXX,3,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXXX,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,SERVICE)

exten => _0XXXXXXXX,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => _0XXXXXXXX,3,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _0XXXXXXXX,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,SERVICE)

exten => _1412XXX!,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => _1412XXX!,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN:4},MAKING,VP,NORMAL,SERVICE)

;Dial Local
exten => _XXXXX!,2,set(__CANCALLOUT=FALSE)
exten => _XXXXX!,4,set(__USER_CALLERID_MAPPING=${CALLERID(num)})

exten => _XXXX,3,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
exten => _XXXX,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,NORMAL,SERVICE)

;service vri
exten => 14120,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 14120,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,VP,NORMAL,SERVICE)

;service technician, helpdesk
exten => 14125,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 14125,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,NORMAL,TECHNICIAN)
exten => 14127,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 14127,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,NORMAL,HELPDESK)

;service emergency
exten => 1412,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 1412,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)
exten => 14121,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 14121,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)

exten => 1669,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 1669,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,SERVICE)
exten => 191,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 191,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,SERVICE)
exten => 199,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 199,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},MAKING,VP,EMG,SERVICE)

;Dial nWise
exten => 14122,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 14122,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)
exten => 020260003,2,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
exten => 020260003,4,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,VP,EMG,SERVICE)

[vrs-ext-dial]
;
; Dial local for trunk
;


exten => _66XXX!,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Goto(vrs-ext-local-structure,${EXTEN:2},1)

[vrs-ext-local-structure]
;
; Dial local 
;


exten => _XXX!,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n(next),Set(__USER_CALL=${CALLERID(num)})

;DIAL TO Client Server
same => n,GotoIf($[${LEN(${EXTEN})} = 13]?step1)
same => n,GotoIf($[${REGEX("^0979045[0-2][0-9]{2}" ${EXTEN})}]?step1)
same => n,GotoIf($[${REGEX("^097904(0[0-9]{3}$|1000$)" ${EXTEN})}]?step1)
same => n,GotoIf($[${REGEX("^090020000[1-9]" ${EXTEN})}]?step1)
same => n,GotoIf($[${REGEX("^02026009[0-3,6-8]" ${EXTEN})}]?step1:external)
same => n(step1),Set(__IS_LOCAL_DIAL=TRUE)
same => n,Set(CALLERID(all)=${IF($["${USER_CALLERID_ALL}" = ""]?${CALLERID(all)}:${USER_CALLERID_ALL})})
same => n,Set(SRC_CALLERID=${IF($["${OPERATOR_CALLERID}" = ""]?${CALLERID(num)}:${OPERATOR_CALLERID})})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dial.php,${SRC_CALLERID},${USER_UNIQUEID})
same => n,Gosub(vrs-check-block,outbound,1(${USER_CALLERID_MAPPING},${DIAL_EXTENSION},${USER_SOURCE_TYPE}))
same => n,Dial(${VRS_CLIENT_TRUNK_OUT}/${EXTEN},60,rTt)
same => n,Hangup()

same => n(external),Set(LOCAL_EXTENSION=${DB(DEVICE/${EXTEN}/dial)})
same => n,GotoIf($["${LOCAL_EXTENSION}" = ""]?vrs-ext-external-call,${EXTEN},1)
same => n,Set(__IS_LOCAL_DIAL=TRUE)
same => n,Set(CALLERID(all)=${IF($["${USER_CALLERID_ALL}" = ""]?${CALLERID(all)}:${USER_CALLERID_ALL})})
same => n,Set(SRC_CALLERID=${IF($["${OPERATOR_CALLERID}" = ""]?${CALLERID(num)}:${OPERATOR_CALLERID})})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dial.php,${SRC_CALLERID},${USER_UNIQUEID})
same => n,Gosub(vrs-check-block,outbound,1(${USER_CALLERID_MAPPING},${DIAL_EXTENSION},${USER_SOURCE_TYPE}))
;same => n,Dial(${LOCAL_EXTENSION},30,rTt)
;same => n(hangup),Hangup()

;;Pushkit iOS
same => n,GotoIf($["${REGEX("pn-type=apple" ${DB(SIP/Registry/${EXTEN})})}"="1"]?ios:normal)
same => n,NoOp(iOS Dial)
same => n(ios),Set(PRESENCE_STATE(CustomPresence:${EXTEN})=available,,)
same => n,AGI(${PUSHKIT_DIR}/pushkit.py, ${DB(SIP/Registry/${EXTEN})})
same => n,Wait(5)
same => n,Dial(Local/${EXTEN}@ext-local,60,rTt)
same => n,Hangup()
same => n(normal),NoOp(Normal Dial)
same => n,Dial(${LOCAL_EXTENSION},30,rTt)
same => n(hangup),Hangup()

exten => h,1,Macro(vrs-hangupcall,)

[vrs-ext-external-call]
;
; Dial external 
;

exten => _X!,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,NoOp(For Override)
same => n,GotoIf($["${CANCALLOUT}" = "TRUE"]?callout)
same => n,NoOp(No Permission to directly call out)
same => n,Hangup(29)

;
;Set Callerid call out
;Callerid range 020260001-020260100 and 020261000-020261099
;
same => n(callout),GotoIf($["${USER_CALLERID_MAPPING}" = ""]?setdefault)
same => n,GotoIf($[${REGEX("^0979045[0-2][0-9]{2}" ${USER_CALLERID_MAPPING})}]?setcaller)
same => n,GotoIf($[${REGEX("^097904(0[0-9]{3}$|1000$)" ${USER_CALLERID_MAPPING})}]?setcaller)
same => n,GotoIf($[${REGEX("^02026000[1-9]|^0202600[1-9][0-9]|^020260100" ${USER_CALLERID_MAPPING})}]?setcaller)
same => n,GotoIf($[${REGEX("^0202610[0-8][0-9]|^02026109[0-9]" ${USER_CALLERID_MAPPING})}]?setcaller:setdefault)
same => n(setcaller),Set(CALLERID(all)=${USER_CALLERID_MAPPING})
same => n,Goto(continue)

same => n(setdefault),Set(CALLERID(all)=${DEFAULTCALLERID})
same => n(continue),NoOp(CDR Billing Here!)
same => n,Set(__IS_EXTERNAL_DIAL=TRUE)
same => n,Set(__DIAL_EXTENSION=${EXTEN})
same => n,AGI(${VRS_AGI_DIR}/curl_get_ttrs_number.php) ; get ttrs number
same => n,ExecIf($["${ttrs_number}" != ""]?Set(CALLERID(all)=${ttrs_number}))  ; set ttrs number
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dial.php,${USER_CALL},${USER_UNIQUEID})
same => n,Gosub(vrs-check-block,outbound,1(${USER_CALLERID_MAPPING},${DIAL_EXTENSION},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-sub-record-dial,s,1())

;; filter tot trunk
same => n,NoOp(number=${CALLERID(num)})
same => n,GotoIf($[${REGEX("^0979045[0-2][0-9]{2}" ${CALLERID(num)})}]?tot_trunk)
same => n,GotoIf($[${REGEX("^097904(0[0-9]{3}$|1000$)" ${CALLERID(num)})}]?tot_trunk)

;Call Outbound Use TOT
;same => n,Set(CALLERID(all)=0979045000);TOT 
;same => n,SipAddHeader(P-Preferred-Identity:sip:0979045000@${VRIVATE_IP_TOT});TOT
;same => n,Dial(${TRUNK_DIAL_TOT_OUT}/${EXTEN},60,rtT) ;TOT

;Call Outbound Use CAT
same => n,Dial(${TRUNK_DIAL_OUT}/${EXTEN},60,rtT) ;CAT

same => n,Hangup()

same => n(tot_trunk),NoOp(${CALLERID(all)})
same => n,SipAddHeader(P-Preferred-Identity:sip:${CALLERID(num)}@${VRIVATE_IP_TOT})
same => n,Set(NETWORKSTATUS=${SIPPEER(trunk_tot2_out,status)})
same => n,GotoIf($[${REGEX("^OK" ${NETWORKSTATUS})}]?trunk1:trunk2)

;same => n(trunk1),Set(CALLERID(all)="020260001" <020260001>) ; use cat 
;same => n(trunk1),Dial(${TRUNK_DIAL_OUT}/${EXTEN},60,rtT) ; use cat 

same => n(trunk1),Dial(${TRUNK_DIAL_TOT_OUT}/${EXTEN},60,rtT) ; use tot
same => n,Hangup()

;same => n(trunk2),Set(CALLERID(all)="020260001" <020260001>) ; use cat
;same => n(trunk2),Dial(${TRUNK_DIAL_OUT}/${EXTEN},60,rtT) ; use cat 

same => n(trunk2),Dial(${TRUNK_DIAL_TOT_OUT_FAILOVER}/${EXTEN},60,rtT) ; use tot
same => n,Hangup()

exten => h,1,Macro(vrs-hangupcall,)

[macro-vrs-hangupcall]
;
; Call Hangup services update
;

exten => s,1,NoOp(Macro vrs-hangupcall)
same => n,GotoIf($["${USER_UNIQUEID}" = "${UNIQUEID}"]?log_service_hangup)
same => n,GotoIf($["${IS_LOCAL_DIAL}" = "TRUE" | "${IS_EXTERNAL_DIAL}" = "TRUE"]?log_dial_hangup:exit_hangup)
same => n(log_service_hangup),AGI(${VRS_AGI_DIR}/agi_update_log_service.php,1,${USER_UNIQUEID},${USER_SOURCE_TYPE},${EPOCH})
same => n,Goto(exit_hangup)
same => n(log_dial_hangup),AGI(${VRS_AGI_DIR}/agi_update_log_dial.php,${DIALSTATUS},${ANSWEREDTIME})
same => n,Goto(exit_hangup)
same => n(exit_hangup),ExecIf($[${REGEX("^80[0-9]{2}" ${CALLERID(num)})}]?SetLastCurrentTimeQueueMember(,Local/${CALLERID(num)}@from-queue/n))
same => n,Hangup
same => n,MacroExit()

[vrs-sub-record-dial]
;
; Voice Record dial
;

exten => s,1,Set(NOW=${EPOCH})
same => n,Set(DAY=${STRFTIME(${NOW},,%d)})
same => n,Set(MONTH=${STRFTIME(${NOW},,%m)})
same => n,Set(YEAR=${STRFTIME(${NOW},,%Y)})
same => n,Monitor(wav,${YEAR}/${MONTH}/${DAY}/${UNIQUEID},m)
same => n,Set(__IS_RECORD_VOICE=TRUE)
same => n,Return()

[vrs-ext-dnd-service]
;
; DND services (dial plan)
;


exten => *79,1,Answer
same => n,NoOp(${CALLERID(num)})
same => n,NoOp(DND off)
same => n,Set(DNDType=${DB(DND/${CALLERID(num)})})
same => n,NoOp(${DNDType:0:4})
same => n,ExecIf($["${DNDType:0:4}" = "DND3"]?SetLastCurrentTimeQueueMember(,Local/${CALLERID(num)}@from-queue/n))
same => n,Wait(1)
same => n,Macro(user-callerid,)
same => n,Noop(Deleting: DND/${AMPUSER} ${DB_DELETE(DND/${AMPUSER})})
same => n,UnpauseQueueMember(,Local/${CALLERID(num)}@from-queue/n)
same => n(hook_1),Playback(beep)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dnd.php,${CALLERID(num)},DND_OFF)
same => n,Set(__EXT_TO=${CALLERID(num)})
same => n,Set(__EXT_FROM=${UNIQUEID})
same => n,Set(__EXT_BODY=DND0)
same => n,Gosub(vrs-ext-dnd-service-api-send-status-sub,s,1())
same => n,Macro(hangupcall,)

exten => *78,1,Answer
same => n,NoOp(${CALLERID(num)})
same => n,NoOp(DND 1)
same => n,Wait(1)
same => n,Macro(user-callerid,)
same => n,Set(DB(DND/${AMPUSER})=DND1:${UNIQUEID})
same => n,PauseQueueMember(,Local/${CALLERID(num)}@from-queue/n)
same => n(hook_1),Playback(beep)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dnd.php,${CALLERID(num)},DND_1)
same => n,Set(__EXT_TO=${CALLERID(num)})
same => n,Set(__EXT_FROM=${UNIQUEID})
same => n,Set(__EXT_BODY=DND1)
same => n,Gosub(vrs-ext-dnd-service-api-send-status-sub,s,1())
same => n,Macro(hangupcall,)

exten => *76,1,Answer
same => n,NoOp(${CALLERID(num)})
same => n,NoOp(DND 2)
same => n,Wait(1)
same => n,Macro(user-callerid,)
same => n,Set(DB(DND/${AMPUSER})=DND2:${UNIQUEID})
same => n,PauseQueueMember(,Local/${CALLERID(num)}@from-queue/n)
same => n(hook_1),Playback(beep)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dnd.php,${CALLERID(num)},DND_2)
same => n,Set(__EXT_TO=${CALLERID(num)})
same => n,Set(__EXT_FROM=${UNIQUEID})
same => n,Set(__EXT_BODY=DND2)
same => n,Gosub(vrs-ext-dnd-service-api-send-status-sub,s,1())
same => n,Macro(hangupcall,)

exten => *73,1,Answer
same => n,NoOp(${CALLERID(num)})
same => n,NoOp(DND 3)
same => n,Wait(1)
same => n,Macro(user-callerid,)
same => n,Set(DB(DND/${AMPUSER})=DND3:${UNIQUEID})
same => n,PauseQueueMember(,Local/${CALLERID(num)}@from-queue/n)
same => n(hook_1),Playback(beep)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dnd.php,${CALLERID(num)},DND_3)
same => n,Set(__EXT_TO=${CALLERID(num)})
same => n,Set(__EXT_FROM=${UNIQUEID})
same => n,Set(__EXT_BODY=DND3);;DND3
same => n,Gosub(vrs-ext-dnd-service-api-send-status-sub,s,1())
same => n,Macro(hangupcall,)


[vrs-ext-dnd-service-api]
;
; DND services for API
;

exten => *79,1,Answer
same => n,NoOp(DND off)
same => n,NoOp(${DND_AGENT})
same => n,NoOp(${UNIQUEID})
same => n,Set(DNDType=${DB(DND/${DND_AGENT})})
same => n,NoOp(${DNDType:0:4})
same => n,ExecIf($["${DNDType:0:4}" = "DND3"]?SetLastCurrentTimeQueueMember(,Local/${DND_AGENT}@from-queue/n))
same => n,Macro(user-callerid,)
same => n,Noop(Deleting: DND/${DND_AGENT} ${DB_DELETE(DND/${DND_AGENT})})
same => n,UnpauseQueueMember(,Local/${DND_AGENT}@from-queue/n)
;same => n,UserEvent(DNDState,userid: ${DND_AGENT},state: off)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dnd.php,${DND_AGENT},DND_OFF)
same => n,Set(__EXT_TO=${DND_AGENT})
same => n,Set(__EXT_FROM=${UNIQUEID})
same => n,Set(__EXT_BODY=DND0)
same => n,Gosub(vrs-ext-dnd-service-api-send-status-sub,s,1())
same => n,Macro(hangupcall,)

exten => *78,1,Answer
same => n,NoOp(DND 1)
same => n,NoOp(${DND_AGENT})
same => n,NoOp(${UNIQUEID})
same => n,Macro(user-callerid,)
same => n,Set(DB(DND/${DND_AGENT})=DND1:${UNIQUEID})
same => n,PauseQueueMember(,Local/${DND_AGENT}@from-queue/n)
;same => n,UserEvent(DNDState,type: DND2,userid: ${DND_AGENT},state: on)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dnd.php,${DND_AGENT},DND_1)
same => n,Set(__EXT_TO=${DND_AGENT})
same => n,Set(__EXT_FROM=${UNIQUEID})
same => n,Set(__EXT_BODY=DND1)
same => n,Gosub(vrs-ext-dnd-service-api-send-status-sub,s,1())
same => n,Macro(hangupcall,)

exten => *76,1,Answer
same => n,NoOp(DND 2)
same => n,NoOp(${DND_AGENT})
same => n,NoOp(${UNIQUEID})
same => n,Macro(user-callerid,)
same => n,Set(DB(DND/${DND_AGENT})=DND2:${UNIQUEID})
same => n,PauseQueueMember(,Local/${DND_AGENT}@from-queue/n)
;same => n,UserEvent(DNDState,type: DND1,userid: ${DND_AGENT},state: on)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dnd.php,${DND_AGENT},DND_2)
same => n,Set(__EXT_TO=${DND_AGENT})
same => n,Set(__EXT_FROM=${UNIQUEID})
same => n,Set(__EXT_BODY=DND2)
same => n,Gosub(vrs-ext-dnd-service-api-send-status-sub,s,1())
same => n,Macro(hangupcall,)

exten => *73,1,Answer
same => n,NoOp(DND 3)
same => n,NoOp(${DND_AGENT})
same => n,NoOp(${UNIQUEID})
same => n,Macro(user-callerid,)
same => n,Set(DB(DND/${DND_AGENT})=DND3:${UNIQUEID})
same => n,PauseQueueMember(,Local/${DND_AGENT}@from-queue/n)
;same => n,UserEvent(DNDState,type: DND2,userid: ${DND_AGENT},state: on)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_dnd.php,${DND_AGENT},DND_3)
same => n,Set(__EXT_TO=${DND_AGENT})
same => n,Set(__EXT_FROM=${UNIQUEID})
same => n,Set(__EXT_BODY=DND3)
same => n,Gosub(vrs-ext-dnd-service-api-send-status-sub,s,1())
same => n,Macro(hangupcall,)

[vrs-ext-dnd-service-api-send-status-sub]
;
; DND Send messages status
;

exten => s,1,NoOp()
same => n,NoOp()
;same => n,Set(EXT1_FROM=${CUT(EXT_FROM,.,1)})
same => n,Set(EXT1_FROM=STATUS)
same => n,Set(MESSAGE(to)=sip:${EXT_TO})
same => n,Set(MESSAGE(from)="${EXT1_FROM}" <sip:${EXT1_FROM}@${SERVER_IP}>)
same => n,Set(MESSAGE(body)=${EXT_BODY})
same => n,MessageSend(${MESSAGE(to)},${MESSAGE(from)})
same => n,Return()


[vrs-ext-main-service]
;
; VP services (dial plan)
;

;Dial Emergency
exten => 1412,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="EMG <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup

;Dial Emergency
exten => 14121,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="EMG <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup


;Dial VRI
exten => 14120,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_VRI},1)
same => n,Hangup

;Dial VRI Web-public, mobile public
exten => 14124,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup

;Dial nWis
exten => 14122,1,NoOp(For Override)
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;;same => n,Set(__VQ_CIDPP="EMG <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup

;Dial nWis
;exten => 020260003,1,NoOp(For Override)
;same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,NoOp(For override)
;same => n,NoOp(For override)
;same => n,NoOp(For override)
;same => n,NoOp(Call to EMG)
;same => n,NoOp(For override)
;same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
;same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
;same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;;same => n,Set(__VQ_CIDPP="EMG <- ")
;same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
;same => n,Wait(2)
;same => n,Playback(vrs-ivvr/waiting_short_1s)
;same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
;same => n,Hangup


;Dial VRS TECHNICIAN
exten => 14125,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to Technician)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="Technician <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_TECHNICIAN},1)
same => n,Hangup

;Dial VRS HELP DESK
exten => 14127,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to Helpdesk)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-helpdesk-sub,s,1())
;same => n,Set(__VQ_CIDPP="HelpDesk <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_HELPDESK},1)
same => n,Hangup

;;Test Service
exten => 14131,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,MAKING,${USER_SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup

;Dial VRS
exten => _1412XXX!,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN:4} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_NORMAL},1)
same => n,Hangup

;Dial VRS 08
exten => _0XXXXXXXXX,1,NoOp()
same => n,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_NORMAL},1)
same => n,Hangup

;Dial VRS 02
exten => _0XXXXXXXX,1,NoOp()
same => n,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_NORMAL},1)
same => n,Hangup

;Dial VRS 1242, 1112, 1150 etc.
exten => _XXXX,1,GotoIf($[${REGEX("^[7-8]0[0-9]{2}" ${EXTEN})}]?hangupcall)
;same => n,GotoIf($["${DB(DEVICE/${EXTEN}/dial)}" != ""]?vrs-ext-local-structure,${EXTEN},1)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_NORMAL},1)
same => n(hangupcall),Hangup

;:::Emergency Thai:::

;Medical Emergency
exten => _1669,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup

;Police Emergency
exten => 191,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup

;Fire Emergency
exten => 199,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup

exten => h,1,Macro(vrs-hangupcall,)

[vrs-ext-main-service-kiosk]
;
; Kiosk services (dial plan)
;

;;;;;;;;;TEST
exten => 14150,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup

exten => 14151,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},EMG,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
;same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
;same => n,Dial(SIP/00045)
same => n,echo()
same => n,Hangup

exten => 14152,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Hangup

exten => 14153,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,891,1)
same => n,Hangup


exten => 14154,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,Set(USER_CALLERID_MAPPING=${CALLERID(num)})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,VRI,${SOURCE_TYPE},NORMAL,TEST)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub-dev,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
;same => n,Set(__VQ_CIDPP="0821210162 <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup
;;;;;;;;;TEST END




;Dial VRI
exten => 14120,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRI)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="VRI <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_VRI},1)
same => n,Hangup

;Dial VRS 08
exten => _0XXXXXXXXX,1,NoOp()
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_NORMAL},1)
same => n,Hangup

;Dial VRS 02
exten => _0XXXXXXXX,1,NoOp()
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_NORMAL},1)
same => n,Hangup

;Dial 4 Digit
exten => _XXXX,1,NoOp()
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to VRS)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
same => n,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
;same => n,Set(__VQ_CIDPP="${EXTEN} <- ")
same => n,AGI(${VRS_AGI_DIR}/agi_set_hangup.php,${CHANNEL})
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_NORMAL},1)
same => n,Hangup

;:::Emergency Thai:::

;Medical Emergency
exten => 1669,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup

;Police Emergency
exten => 191,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup

;Fire Emergency
exten => 199,1,Answer
same => n,Set(USER_CALLERID_MAPPING=${DEFAULTCALLERID})
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(For override)
same => n,NoOp(Call to EMG)
same => n,NoOp(For override)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-main-sub,s,1())
;same => n,Set(__VQ_CIDPP="${EXTEN} (EMG) <- ")
same => n,Wait(2)
same => n,Playback(vrs-ivvr/waiting_short_1s)
same => n,Goto(ext-queues,${QUEUE_VRS_EMG},1)
same => n,Hangup

exten => h,1,GotoIf($["${USER_UNIQUEID}" != "${UNIQUEID}"]?next)
same => n,AGI(${VRS_AGI_DIR}/agi_update_log_service.php,1,${USER_UNIQUEID},${USER_SOURCE_TYPE},${EPOCH})
same => n(next),Hangup

[vrs-ext-echo]
;
; Echo test
;
exten => _999999,1,Answer
same => n,Echo()
same => n,Hangup

[vrs-inbound-tot_trunk01]

; to new tot trunk
exten => _X!,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_check_receiving.php)
same => n,Gosub(check-block-by-system,s,1(${CALLERID(num)})) ; block by system
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,${receiving_type},NORMAL,SERVICE)
; same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,MOBILE,NORMAL,SERVICE)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,AGI(${VRS_AGI_DIR}/curl_get_ttrs_number_detail.php)
same => n,Set(CALLERID(all)="${ttrs_number_name}" <${CALLERID(num)}>)
;same => n,Set(CALLERID(all)="${CALLERID(num)}" <${CALLERID(num)}>)
same => n,Gosub(vrs-check-service-off-receiving-sub,s,1())
same => n,AGI(agi://127.0.0.1/check_ttrsnumber)
same => n,NoOp(DEST_IS_TTRS_NUMBER=${DEST_IS_TTRS_NUMBER})
same => n,Set(__USER_CALL_TO_TTRS_NUMBER=${DEST_IS_TTRS_NUMBER})
; same => n,ExecIf($["${DEST_IS_TTRS_NUMBER}" = "1"]?Set(CHANNEL(musicclass)=call-waiting-classic))

same => n,GotoIf($["${receiving_type}" = "TEXT"]?enterQueue)
same => n,Gosub(vrs-check-service-off-receiving-sub,s,1())
same => n,ExecIf($["${DEST_IS_TTRS_NUMBER}" = "1"]?Set(CHANNEL(musicclass)=call-waiting-classic))

same => n(enterQueue),ExecIf($["${receiving_type}" = "MOBILE"]?Goto(ext-queues,810,1):Goto(ext-queues,910,1))

; same => n,Goto(ext-queues,810,1)
same => n,Hangup()

exten => 0979045045,1,NoOp(For IVR)
same => n,Dial(SIP/020260095@ivr_38,30,rtT)
same => n,Hangup()

exten => 0979045046,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,Gosub(check-block-by-system,s,1(${CALLERID(num)})) ; block by system
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,MOBILE,NORMAL,TEST)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,AGI(${VRS_AGI_DIR}/curl_get_ttrs_number_detail.php)
same => n,Set(CALLERID(all)="${ttrs_number_name}" <${CALLERID(num)}>)
;same => n,Set(CALLERID(all)="${CALLERID(num)}" <${CALLERID(num)}>)
;same => n,Gosub(vrs-check-service-off-receiving-sub,s,1())
same => n,AGI(agi://127.0.0.1/check_ttrsnumber)
same => n,NoOp(DEST_IS_TTRS_NUMBER=${DEST_IS_TTRS_NUMBER})
same => n,Set(__USER_CALL_TO_TTRS_NUMBER=${DEST_IS_TTRS_NUMBER})
same => n,ExecIf($["${DEST_IS_TTRS_NUMBER}" = "1"]?Set(CHANNEL(musicclass)=call-waiting-classic))
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup()

exten => 0979045047,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Dial(SIP/020260094@ivr_out,30,rtT)
same => n,Hangup()

[vrs-inbound-cat_trunk01]
;
; From receiving trunk
;

;To IVR 210.4.150.45
exten => 020260095,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
;same => n,Dial(SIP/020260095@ivr_out,30,rtT)
same => n,Dial(SIP/020260095@ivr_200,30,rtT)
same => n,Hangup()

;To IVR 210.4.150.45 TEST
exten => 020260094,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,Dial(SIP/020260094@ivr_out,30,rtT)
same => n,Hangup()

; AGI ARGV Format
; (destination_number, call_type, source_type, priority_type, group_type)
; Ex: (${EXTEN}, 1, 1, 2, 1)
;
; call_type : 1=MAKING, 2=RECEIVING, 3=VRI
; source_type : 1=VP, 2=KIOSK, 3=MOBILE, 4=WEB, 5=CAPTION
; priority_type : 1=NORMAL, 2=EMG
; group_type : 1=SERVICE, 2=HELPDESK, 3=TECHNICIAN, 4=TEST

;exten => 020260097,1,NoOp(For Override)
;same => n,NoOp(For Override)
;same => n,Set(__USER_ID=${CALLERID(num)})
;same => n,Set(__USER_UNIQUEID=${UNIQUEID})
;same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,RECEIVING,VP,NORMAL,TEST)
;same => n,Set(CALLERID(all)="Recieving <- ${CALLERID(num)} (Outside)" <${CALLERID(num)}>)
;same => n,Goto(ext-queues,004,1)

;VRS Receiving default
exten => 020260001,1,NoOp(For Override)
same => n,NoOp(VRS Receiving default
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,RECEIVING,MOBILE,NORMAL,SERVICE)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-receiving-sub,s,1())
same => n,Set(CALLERID(all)="${CALLERID(num)}" <${CALLERID(num)}>)
same => n,Goto(ext-queues,810,1)
same => n,Hangup()

;Inbound text services
exten => 020260002,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,RECEIVING,TEXT,NORMAL,SERVICE)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},TEXT))
;same => n,Gosub(vrs-check-service-off-receiving-sub,s,1())
;same => n,Set(CALLERID(all)="${CALLERID(num)}" <${CALLERID(num)}>)
same => n,Goto(ext-queues,910,1)
same => n,Hangup()

;VRS Receiving default
exten => 020260003,1,NoOp(For Override)
same => n,NoOp(VRS Receiving default
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,,RECEIVING,MOBILE,NORMAL,SERVICE)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-receiving-sub,s,1())
same => n,Set(CALLERID(all)="${CALLERID(num)}" <${CALLERID(num)}>)
same => n,Goto(ext-queues,810,1)
same => n,Hangup()

;Inbound VRS All
exten => _X!,1,NoOp(For Override)
same => n,NoOp(Inbound VRS All)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,Gosub(check-block-by-system,s,1(${CALLERID(num)})) ; block by system
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,MOBILE,NORMAL,SERVICE)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Gosub(vrs-check-service-off-receiving-sub,s,1())
same => n,Set(CALLERID(all)="${CALLERID(num)}" <${CALLERID(num)}>)
same => n,Goto(ext-queues,810,1)
same => n,Hangup()

;Inbound VRS VP 020260004 - 020260090
;exten => _0202600XX,1,NoOp(For Override)
;same => n,NoOp(Inbound VRS VP 020260004 - 020260090)
;same => n,Set(__USER_ID=${CALLERID(num)})
;same => n,Set(__USER_UNIQUEID=${UNIQUEID})
;same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,VP,NORMAL,SERVICE)
;same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
;same => n,Gosub(vrs-check-service-off-receiving-sub,s,1())
;same => n,Set(CALLERID(all)="${CALLERID(num)}" <${CALLERID(num)}>)
;same => n,Goto(ext-queues,810,1)
;same => n,Hangup()

;Inbound VRS VP 020261000 - 020261099
;exten =>  _0202610XX,1,NoOp(For Override)
;same => n,NoOp(Inbound VRS VP 020261000 - 020261099)
;same => n,Set(__USER_ID=${CALLERID(num)})
;same => n,Set(__USER_UNIQUEID=${UNIQUEID})
;same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,VP,NORMAL,SERVICE)
;same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
;same => n,Gosub(vrs-check-service-off-receiving-sub,s,1())
;same => n,Set(CALLERID(all)="${CALLERID(num)}" <${CALLERID(num)}>)
;same => n,Goto(ext-queues,810,1)
;same => n,Hangup()


;nWis all
;exten => _X!,1,NoOp(For Override)
;same => n,NoOp(For Override)
;same => n,Set(__USER_ID=${CALLERID(num)})
;same => n,Set(__USER_UNIQUEID=${UNIQUEID})
;same => n,AGI(${VRS_AGI_DIR}/agi_set_blocklist.php)
;same => n,Dial(SIP/ttrs@trunk_nWise-production-onlyvoice,30,rtT)
;same => n(forcehangup),Hangup()

;test ems
;exten => 020260091,1,NoOp(TEST EMS)
;same => n,NoOp(For Override)
;same => n,Set(__USER_ID=${CALLERID(num)})
;same => n,Set(__USER_UNIQUEID=${UNIQUEID})
;same => n,Dial(SIP/166927@ems_211,30,rtT)
;same => n,Hangup()

;test ems
;exten => 020260096,1,NoOp(TEST EMS)
;same => n,NoOp(For Override)
;same => n,Set(__USER_ID=${CALLERID(num)})
;same => n,Set(__USER_UNIQUEID=${UNIQUEID})
;same => n,Dial(SIP/020260096@ems_211,30,rtT)
;same => n,Hangup()

exten => 020260096,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_service.php,${EXTEN},RECEIVING,MOBILE,NORMAL,TEST)
same => n,AGI(${VRS_AGI_DIR}/agi_set_log_device.php)
same => n,Gosub(vrs-check-block,inbound,1(${CALLERID(num)},${USER_OUT_MAPPING},${USER_SOURCE_TYPE}))
same => n,Set(CALLERID(all)="Recieving <- ${CALLERID(num)} (Outside)" <${CALLERID(num)}>)
;same => n,Goto(ext-queues,${QUEUE_VRS_TTRS_TEST},1)
same => n,Goto(ext-queues,${QUEUE_VRS_TEST},1)
same => n,Hangup()

exten => 020260100,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
same => n,Dial(SIP/166912@ems_inet_227,30,rtT)
same => n,Hangup()

exten => 020260099,1,NoOp(For Override)
same => n,NoOp(For Override)
same => n,Answer()
same => n,Set(__USER_ID=${CALLERID(num)})
same => n,Set(__USER_UNIQUEID=${UNIQUEID})
;same => n,Dial(SIP/166972@ems_idems_callcenter03,30,rtT)
same => n,Dial(SIP/166900@ems_idems_callcenter15,30,rtT)
same => n,Hangup()

[nwise-inbound-trunk01]
;
;nWise calls out
;
include => vrs-ext-local-structure

exten => _X!,1,set(__CANCALLOUT=TRUE)
exten => _X!,2,set(__USER_CALLERID_MAPPING=020260003)



[vrs-message-outgoing]
;
; VRS API Send messages
;
exten => MSG,1,NoOp()
same => n,Set(MESSAGE(to)=${TO})
same => n,Set(MESSAGE(from)=${FROM})
same => n,GotoIf($["${MESSAGE_BODY}" = ""]?hangup)
same => n,Set(MESSAGE(body)=${MESSAGE_BODY})

same => n,Set(LOCAL_EXTENSION=${DB(DEVICE/${TO:4}/dial)})
same => n,GotoIf($[${REGEX("^00001681800[0-9]{2}" ${TO:4})}]?step1);Check WebRTC Local
same => n,GotoIf($["${LOCAL_EXTENSION}" != ""]?step1:step2)

;same => n,GotoIf($[${LEN(${TO:4})} = 13]?step2);;TO=sip:1234567890123

same => n(step1),MessageSend(${MESSAGE(to)},${MESSAGE(from)})
same => n,NoOp(Message Send Status : ${MESSAGE_SEND_STATUS})
same => n,hangup

same => n(step2),Set(ACTUALFROM=${CUT(MESSAGE(from),@,1)})
same => n,Set(ACTUALFROM1=${CUT(ACTUALFROM,<,2)})

; filter webrtc
same => n,NoOp(check webrtc ${TO:4})
same => n,GotoIf($[${REGEX("^99[0-9]{13}" ${TO:4})}]?to34) ; idcard
same => n,GotoIf($[${REGEX("^99[0-9]{10}" ${TO:4})}]?to34) ; ttrs number

;same => n,MessageSend(${MESSAGE(to)}@${VRS_MOBILE_CLIENT_IP},${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP})
same => n,MessageSend(${MESSAGE(to)}@vrs_client_out,${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP})
;same => n,MessageSend(${MESSAGE(to)}@${VRS_MOBILE_CLIENT_IP},${MESSAGE(from)})
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,hangup

; to serv 34
same => n(to34),MessageSend(${MESSAGE(to)}@${VRS_WEBRTC_CLIENT_IP},${ACTUALFROM}@${VRS_WEBRTC_CLIENT_IP})
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,hangup

[astsms]
;
; SMS (messaging or SIP Messaging)
;
exten => _X.,1,NoOp(SMS receiving dialplan invoked)
exten => _X.,n,NoOp(To ${MESSAGE(to)})
exten => _X.,n,NoOp(From ${MESSAGE(from)})
exten => _X.,n,NoOp(Body ${MESSAGE(body)})
;exten => _X.,n,GotoIf($[${REGEX("^<rtt.+<\/rtt>" ${MESSAGE(body)})}]?isrtt)
exten => _X.,n,Set(ACTUALTO=${CUT(MESSAGE(to),@,1)})
exten => _X.,n,MessageSend(${ACTUALTO},${MESSAGE(from)})
exten => _X.,n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
exten => _X.,n,GotoIf($["${MESSAGE_SEND_STATUS}" != "SUCCESS"]?sendfailedmsg)
exten => _X.,n,Hangup()
exten => _X.,n(sendfailedmsg),NoOp("[ข้อความจากระบบ] ส่งข้อความไม่สำเร็จ!!")
;exten => _X.,n(sendfailedmsg),Set(MESSAGE(body)="[ข้อความจากระบบ] ส่งข้อความไม่สำเร็จ!!")
;exten => _X.,n,Set(ME_1=${CUT(MESSAGE(from),<,2)})
;exten => _X.,n,Set(ACTUALFROM=${CUT(ME_1,@,1)})
;exten => _X.,n,MessageSend(${ACTUALFROM},ServiceCenter)
exten => _X.,n(isrtt),Hangup()


;To vrs client (13)
exten => _XXXXXXXXXXXXX,1,NoOp(Send to vrs client)
same => n,NoOp(To ${MESSAGE(to)})
same => n,NoOp(From ${MESSAGE(from)})
same => n,NoOp(Body ${MESSAGE(body)})
;same => n,GotoIf($[${REGEX("^<rtt.+<\/rtt>" ${MESSAGE(body)})}]?isrtt)
same => n,Set(SRT1=${CUT(MESSAGE(from),@,1)})
same => n,Set(ACTUALFROM=${CUT(SRT1,:,2)})
same => n,Set(ACTUALTO=${CUT(MESSAGE(to),@,1)})

same => n,Set(LOCAL_EXTENSION=${DB(DEVICE/${EXTEN}/dial)})
same => n,GotoIf($[${REGEX("^00001681800[0-9]{2}" ${EXTEN})}]?step1);Check WebRTC Local
same => n,GotoIf($["${LOCAL_EXTENSION}" != ""]?step1)

;same => n,MessageSend(sip:${EXTEN}@${VRS_MOBILE_CLIENT_IP},"${ACTUALFROM}" <sip:${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP}>)
same => n,MessageSend(sip:${EXTEN}@vrs_client_out,"${ACTUALFROM}" <sip:${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP}>)
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,hangup

same => n(step1),MessageSend(${ACTUALTO},${MESSAGE(from)})
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n(isrtt),hangup

;To vrs client WebRTC (15)
exten => _XXXXXXXXXXXXXXX,1,NoOp(Send to vrs client WebRTC)
same => n,NoOp(To ${MESSAGE(to)})
same => n,NoOp(From ${MESSAGE(from)})
same => n,NoOp(Body ${MESSAGE(body)})
;same => n,GotoIf($[${REGEX("^<rtt.+<\/rtt>" ${MESSAGE(body)})}]?isrtt)
same => n,Set(SRT1=${CUT(MESSAGE(from),@,1)})
same => n,Set(ACTUALFROM=${CUT(SRT1,:,2)})
same => n,Set(ACTUALTO=${CUT(MESSAGE(to),@,1)})

same => n,Set(LOCAL_EXTENSION=${DB(DEVICE/${EXTEN}/dial)})
same => n,GotoIf($["${LOCAL_EXTENSION}" != ""]?step1)

;same => n,MessageSend(sip:${EXTEN}@${VRS_WEBRTC_CLIENT_IP},"${ACTUALFROM}" <sip:${ACTUALFROM}@${VRS_WEBRTC_CLIENT_IP}>)
same => n,MessageSend(sip:${EXTEN}@vrs_client_out,"${ACTUALFROM}" <sip:${ACTUALFROM}@${VRS_WEBRTC_CLIENT_IP}>)
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,hangup

same => n(step1),MessageSend(${ACTUALTO},${MESSAGE(from)})
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n(isrtt),hangup

;To vrs client (vip)
exten => _02026XXXX,1,NoOp(Send to vrs client)
same => n,NoOp(To ${MESSAGE(to)})
same => n,NoOp(From ${MESSAGE(from)})
same => n,NoOp(Body ${MESSAGE(body)})
;same => n,GotoIf($[${REGEX("^<rtt.+<\/rtt>" ${MESSAGE(body)})}]?isrtt)
same => n,Set(SRT1=${CUT(MESSAGE(from),@,1)})
same => n,Set(ACTUALFROM=${CUT(SRT1,:,2)})
same => n,Set(ACTUALTO=${CUT(MESSAGE(to),@,1)})

same => n,Set(LOCAL_EXTENSION=${DB(DEVICE/${EXTEN}/dial)})
same => n,GotoIf($["${LOCAL_EXTENSION}" != ""]?step1)

;same => n,MessageSend(sip:${EXTEN}@${VRS_MOBILE_CLIENT_IP},"${ACTUALFROM}" <sip:${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP}>)
same => n,MessageSend(sip:${EXTEN}@vrs_client_out,"${ACTUALFROM}" <sip:${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP}>)
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,hangup

same => n(step1),MessageSend(${ACTUALTO},${MESSAGE(from)})
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n(isrtt),hangup

;To vrs client TTRS Number
exten => _097904XXXX,1,NoOp(SMS receiving from ${EXTEN})
same => n,NoOp(To ${MESSAGE(to)})
same => n,NoOp(From ${MESSAGE(from)})
same => n,NoOp(Body ${MESSAGE(body)})
same => n,Set(ACTUALTO=${CUT(MESSAGE(to),@,1)})
same => n,Set(ACTUALFROM=${CUT(MESSAGE(from),@,1)})
;same => n,MessageSend(${ACTUALTO}@${VRS_WEBRTC_CLIENT_IP},${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP}>)
same => n,MessageSend(${ACTUALTO}@vrs_client_out,${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP}>)
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,GotoIf($["${MESSAGE_SEND_STATUS}" != "SUCCESS"]?sendfailedmsg)
same => n,Hangup()
same => n(sendfailedmsg),NoOp("[ข้อความจากระบบ] ส่งข้อความไม่สำเร็จ!!")
same => n,Hangup()

;To vrs client WebRTC TTRS Number
exten => _99097904XXXX,1,NoOp(SMS receiving from ${EXTEN})
same => n,NoOp(To ${MESSAGE(to)})
same => n,NoOp(From ${MESSAGE(from)})
same => n,NoOp(Body ${MESSAGE(body)})
same => n,Set(ACTUALTO=${CUT(MESSAGE(to),@,1)})
same => n,Set(ACTUALFROM=${CUT(MESSAGE(from),@,1)})
;same => n,MessageSend(${ACTUALTO}@${VRS_WEBRTC_CLIENT_IP},${ACTUALFROM}@${VRS_WEBRTC_CLIENT_IP})
same => n,MessageSend(${ACTUALTO}@vrs_client_out,${ACTUALFROM}@${VRS_WEBRTC_CLIENT_IP})
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,GotoIf($["${MESSAGE_SEND_STATUS}" != "SUCCESS"]?sendfailedmsg)
same => n,Hangup()
same => n(sendfailedmsg),NoOp("[ข้อความจากระบบ] ส่งข้อความไม่สำเร็จ!!")
same => n,Hangup()

exten => _090020000X,1,NoOp(SMS receiving from ${EXTEN})
same => n,NoOp(To ${MESSAGE(to)})
same => n,NoOp(From ${MESSAGE(from)})
same => n,NoOp(Body ${MESSAGE(body)})
same => n,Set(ACTUALTO=${CUT(MESSAGE(to),@,1)})
same => n,Set(ACTUALFROM=${CUT(MESSAGE(from),@,1)})
;same => n,MessageSend(${ACTUALTO}@${VRS_MOBILE_CLIENT_IP},${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP})
same => n,MessageSend(${ACTUALTO}@vrs_client_out,${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP})
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,GotoIf($["${MESSAGE_SEND_STATUS}" != "SUCCESS"]?sendfailedmsg)
same => n,Hangup()
same => n(sendfailedmsg),NoOp("[ข้อความจากระบบ] ส่งข้อความไม่สำเร็จ!!")
same => n,Hangup()

exten => _5000XXX,1,NoOp(Send to vrs client)
same => n,NoOp(To ${MESSAGE(to)})
same => n,NoOp(From ${MESSAGE(from)})
same => n,NoOp(Body ${MESSAGE(body)})
same => n,Set(SRT1=${CUT(MESSAGE(from),@,1)})
same => n,Set(ACTUALFROM=${CUT(SRT1,:,2)})
same => n,Set(ACTUALTO=${CUT(MESSAGE(to),@,1)})
same => n,Set(LOCAL_EXTENSION=${DB(DEVICE/${EXTEN}/dial)})
same => n,GotoIf($["${LOCAL_EXTENSION}" != ""]?local)
;same => n,MessageSend(sip:${EXTEN}@${VRS_MOBILE_CLIENT_IP},"${ACTUALFROM}" <sip:${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP}>)
same => n,MessageSend(sip:${EXTEN}@vrs_client_out,"${ACTUALFROM}" <sip:${ACTUALFROM}@${VRS_MOBILE_CLIENT_IP}>)
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,hangup

same => n(local),MessageSend(${ACTUALTO},${MESSAGE(from)})
same => n,NoOp(Send status is ${MESSAGE_SEND_STATUS})
same => n,hangup